

const PassFunction = (props) => {
    return (
        <div className="container">
            <button onClick={props.childBtnFunc}>Submit</button>
        </div>
    );
};

export default PassFunction;