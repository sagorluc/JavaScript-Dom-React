var numbers = [1,2,3,4,5,6]

var res = numbers.map(function mul(num){
    return 2 * num;
});

console.log(numbers); // main array change hobe nah
console.log(res); // every element 2 diye multiply hobe